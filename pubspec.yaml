name: client_ai_app
description: AI-powered client reply assistant with overlay

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_overlay_window: ^0.4.3
  shared_preferences: ^2.2.2
  permission_handler: ^11.1.0
  flutter_foreground_task: ^6.1.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
